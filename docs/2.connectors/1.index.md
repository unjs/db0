---
icon: gravity-ui:plug-connection
---

# Connectors

> You can use DB0 api with the connector of your own choice.

Currently supported connectors:

- [Bun](/connectors/bun)
- [Cloudflare D1](/connectors/cloudflare)
- [LibSQL](/connectors/libsql)
- [Neon](/connectors/neon)
- [Neon Instant](/connectors/neon-instant)
- [PlanetScale](/connectors/planetscale)
- [PostgreSQL](/connectors/postgresql)
- [Prisma Postgres](/connectors/postgresql#prisma-postgres)
- [MySQL](/connectors/mysql)
- [SQLite](/connectors/sqlite)

::read-more{to="https://github.com/unjs/db0/issues/32"}
See [unjs/db0#32](https://github.com/unjs/db0/issues/32) for the list of upcoming connectors.
::

## Connector dependencies

DB0 itself has no runtime dependencies. Connectors that talk to a third-party database library (`better-sqlite3`, `pg`, `mysql2`, `@libsql/client`, ...) import it **lazily and dynamically** on first use, so you only need to install what the connectors you actually use require. Each connector page lists its own install command.

| Connector                                                                          | Package                                                                        |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| [`better-sqlite3`](/connectors/sqlite#better-sqlite3)                              | [`better-sqlite3`](https://www.npmjs.com/package/better-sqlite3)               |
| [`bun-sqlite`](/connectors/bun)                                                    | — (built into Bun)                                                             |
| [`cloudflare-d1`](/connectors/cloudflare)                                          | —                                                                              |
| [`cloudflare-hyperdrive-mysql`](/connectors/cloudflare#hyperdrive-mysql)           | [`mysql2`](https://www.npmjs.com/package/mysql2)                               |
| [`cloudflare-hyperdrive-postgresql`](/connectors/cloudflare#hyperdrive-postgresql) | [`pg`](https://www.npmjs.com/package/pg)                                       |
| [`libsql`](/connectors/libsql)                                                     | [`@libsql/client`](https://www.npmjs.com/package/@libsql/client)               |
| [`mysql2`](/connectors/mysql)                                                      | [`mysql2`](https://www.npmjs.com/package/mysql2)                               |
| [`neon`](/connectors/neon)                                                         | [`@neondatabase/serverless`](https://www.npmjs.com/package/@neondatabase/serverless) |
| [`neon-instant`](/connectors/neon-instant)                                         | [`@neondatabase/serverless`](https://www.npmjs.com/package/@neondatabase/serverless), [`neon-new`](https://www.npmjs.com/package/neon-new) _(optional)_ |
| [`node-sqlite`](/connectors/sqlite#node-sqlite)                                    | — (built into Node.js and Deno)                                                |
| [`pglite`](/connectors/pglite)                                                     | [`@electric-sql/pglite`](https://www.npmjs.com/package/@electric-sql/pglite)   |
| [`planetscale`](/connectors/planetscale)                                           | [`@planetscale/database`](https://www.npmjs.com/package/@planetscale/database) |
| [`postgresql`](/connectors/postgresql)                                             | [`pg`](https://www.npmjs.com/package/pg)                                       |
| [`sqlite3`](/connectors/sqlite#sqlite3)                                            | [`sqlite3`](https://www.npmjs.com/package/sqlite3)                             |

If your bundler or runtime cannot resolve the dynamic import (or you simply prefer a static top-level import), every such connector accepts a `lib` option:

```ts
import { createDatabase } from "db0";
import postgresql from "db0/connectors/postgresql";
import * as pg from "pg";

const db = createDatabase(
  postgresql({
    url: process.env.DATABASE_URL,
    lib: pg, // the module itself
    // lib: () => import("pg"), // or a (sync/async) function returning it
  }),
);
```

### Introspecting dependencies

Each connector module exports a `CONNECTOR_DEPENDENCIES` object describing what it needs, keyed by the option used to provide it. The same data is aggregated for all built-in connectors as `connectorDependencies`, so frameworks can check (or install) what a configured connector requires:

```ts
import { connectorDependencies } from "db0";

connectorDependencies["postgresql"];
// { lib: { name: "pg", version: "^8" } }
```

`optional: true` means the package is only needed for some connector features or configurations. Connectors with no third-party dependencies are absent from the map.
