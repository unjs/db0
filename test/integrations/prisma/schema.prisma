datasource db {
  provider = "sqlite"
}

generator client {
  provider        = "prisma-client"
  output          = "./dist"
}

model User {
  id      Int      @id @default(autoincrement())
  name    String

  @@map("users")
}
